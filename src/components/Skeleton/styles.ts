import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    backgroundColor: 'lightgray',
    overflow: 'hidden',
    width: '100%',
  },

  sprite: {
    width: 142,
    height: 100,
    resizeMode: 'stretch',
  },
})
